name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Run automation framework
      run: mvn clean test -Dsuite=testng.xml -Djava.util.concurrent.ForkJoinPool.common.parallelism=2 -Dthreadcount=2 -Dmaven.test.failure.ignore=true


    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v4.6.2
      with: 
        name: test report
        path: target/extentReports/extent-report.html
        
